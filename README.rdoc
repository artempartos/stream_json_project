== README

* clone repo

* bundle install

* rake db:create db:migrate

* rake app:fill_product

* run `unicorn-rails`

* curl -i http://localhost:8080/

* можно менять параметр sleep в контроллере чтобы посмотреть как имитируется задержка при стриме
